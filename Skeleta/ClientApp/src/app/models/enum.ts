export enum CrudAction {
  Create = "Create",
  Read = "Read",
  Update = "Update",
  Delete = "Delete"
}

export enum Gender {
  None,
  Female,
  Male
}

export enum Priority {
  Critical,
  High,
  Medium,
  Low
}

export enum Status {
  New,
  Rejected,
  Active,
  Resolved,
  Completed,
  Closed
}
